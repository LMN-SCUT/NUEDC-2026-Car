# NUEDC 2026 Car

全国大学生电子设计竞赛控制类小车项目代码仓库。

## 模块

- `vision-k230/`：K230 视觉采集、识别与观测结果输出。
- `controller-stm32/`：STM32 候选主控的驱动与控制代码。
  - `controller-stm32/h60-f407-line-car/`：OpenCTR H60 / STM32F407VET6
    小车工程，包含四路电机、四路编码器、I2C 八路灰度、HC-05、
    堵转保护、H题环形地图循迹和四轮编码器 PI 速度内环。
- `controller-ti/`：LP-MSPM0G3507 候选主控的驱动与控制代码。
- `shared/protocol/`：K230 与最终主控共享的通信协议、字段定义和测试帧。
- `tests/`：串口、协议和联调测试。
- `docs/`：编译、烧录、运行和验收说明。

## 协作规则

1. `main` 只保留经过验证的稳定版本，不直接在 `main` 上开发。
2. 每项工作从 `main` 创建短期分支，例如 `feature/k230-detection`、`feature/ti-uart`。
3. 完成功能和基本测试后发起 Pull Request，由至少一名队友查看后合并。
4. 一次提交只处理一类改动；提交信息说明改动内容和验证硬件。
5. SDK、安装程序、编译输出、密钥和大型资料不进入仓库。

## 当前阶段

- 建立仓库与三人协作流程。
- 整理 K230 视觉遗产，分类为“可复用 / 待修复 / 废弃”。
- K230—主控统一协议 v1 已完成 Python/C 自动测试和双主控实机验证。
- STM32F407（UART4：PC10/PC11）与 MSPM0G3507（UART1：PA8/PA9）均已完成双向通信、命令 ACK、失联保护、重新接线自动恢复和异常流恢复测试。
- H60 / STM32F407VET6 小车已完成四路电机方向、编码器计数、HC-05
  遥测和 I2C 八路灰度基础验证；当前固件已接入灰度PD外环、四轮编码器PI
  内环、A点离开/重遇门控及30秒兜底停车，等待H题实车地图调参。
- 通信层进入稳定基线阶段；下一阶段把视觉算法输出接入统一观测接口，并分别补充两种主控的题目相关驱动。
